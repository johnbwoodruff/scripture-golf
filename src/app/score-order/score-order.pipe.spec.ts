import { ScoreOrderPipe } from './score-order.pipe';

describe('ScoreOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
